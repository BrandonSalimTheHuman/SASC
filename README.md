Click Code --> Download ZIP
Extract the folder, then open SASC-main using vs code

Command to run: python csv_uploader/app.py

Then, open the local link it shows in the terminal using Ctrl + Click

Note: Database contains dummy data for everything except 24.2, so ignore those ones
